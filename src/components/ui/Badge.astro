---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center rounded-full border border-border-glow bg-bg-card px-3 py-1 text-xs font-medium text-text-secondary shadow-[0_0_12px_rgba(0,212,255,0.08)]',
    className,
  ]}
>
  <slot />
</span>
