---
interface Props {
  as?: 'span' | 'h1' | 'h2' | 'h3' | 'p';
  class?: string;
}

const { as: Tag = 'span', class: className = '' } = Astro.props;
---

<Tag class:list={['bg-gradient-to-r from-accent-cyan to-accent-purple bg-clip-text text-transparent', className]}>
  <slot />
</Tag>
