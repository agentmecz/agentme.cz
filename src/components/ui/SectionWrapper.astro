---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = '', id } = Astro.props;
---

<section id={id} class:list={['mx-auto w-full max-w-[1200px] px-6 py-24 md:py-32 lg:py-40', className]}>
  <slot />
</section>
