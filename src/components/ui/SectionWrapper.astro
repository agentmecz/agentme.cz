---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = '', id } = Astro.props;
---

<section id={id} class:list={['mx-auto w-full max-w-[1200px] px-6 py-16 md:py-20 lg:py-24', className]}>
  <slot />
</section>
