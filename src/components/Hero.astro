---
import SectionWrapper from './ui/SectionWrapper.astro';
import Badge from './ui/Badge.astro';
import GradientText from './ui/GradientText.astro';
import Button from './ui/Button.astro';
import GitHubStarButton from './GitHubStarButton.astro';
import HeroNetwork from './HeroNetwork.astro';
import { hero, site } from '../data/content';
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Background glow -->
  <div
    class="pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_60%_50%_at_50%_40%,rgba(0,212,255,0.08),transparent_70%)]"
    aria-hidden="true"
  />

  <!-- Network animation -->
  <HeroNetwork />

  <SectionWrapper class="relative z-10 text-center">
    <div class="flex flex-col items-center gap-6">
      <Badge>{hero.badge}</Badge>

      <h1 class="max-w-4xl text-4xl font-bold leading-[1.1] tracking-tight md:text-6xl lg:text-7xl">
        <GradientText as="span">{hero.headline}</GradientText>
      </h1>

      <p class="max-w-2xl text-lg leading-relaxed text-text-secondary md:text-xl">
        {hero.subheadline}
      </p>

      <p class="max-w-xl text-sm text-text-muted italic">
        "The HTTP of agent-to-agent commerce"
      </p>

      <!-- GEO: extractable definition block for AI engines -->
      <p class="sr-only">
        AgentMesh is an open-source protocol and trust layer for AI agent commerce. It enables agent-to-agent discovery via semantic search over Kademlia DHT, on-chain trust scores combining reputation, stake, and endorsements, and USDC micropayments via x402, escrow, and streaming on Base L2. Includes a Bridge module for local AI agents (Claude Code, ChatGPT) to offer services through the network. MIT licensed, deployed on Base Sepolia.
      </p>

      <div class="flex flex-wrap items-center justify-center gap-4 pt-4">
        <GitHubStarButton size="large" />
        <Button href={site.docsUrl} variant="secondary" target="_blank" rel="noopener noreferrer">
          {hero.ctaSecondary}
        </Button>
      </div>
    </div>
  </SectionWrapper>
</section>
