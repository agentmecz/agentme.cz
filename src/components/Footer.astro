---
import { t, type Locale } from '../data/i18n';

interface Props { locale: Locale }
const { locale } = Astro.props;
const { site, footer } = t(locale);
---

<footer class="border-t border-border">
  <div class="mx-auto max-w-[1200px] px-6 py-8">
    <div class="flex flex-col items-center gap-4 sm:flex-row sm:justify-between">
      <span class="text-sm font-semibold text-text-secondary">AgentMe</span>

      <nav class="flex flex-wrap items-center justify-center gap-x-6 gap-y-2" aria-label="Footer">
        <a
          href={site.repoUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="text-sm text-text-secondary transition-colors hover:text-text-primary"
        >
          GitHub
        </a>
        <a
          href={site.docsUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="text-sm text-text-secondary transition-colors hover:text-text-primary"
        >
          Docs
        </a>
        <a
          href="/llms.txt"
          class="text-sm text-text-secondary transition-colors hover:text-text-primary"
        >
          llms.txt
        </a>
      </nav>

      <span class="text-xs text-text-secondary">{footer.copyright}</span>
    </div>

    <div class="border-t border-border mt-6 pt-6 pb-2 text-center">
      <p class="text-sm text-text-secondary mb-2">Built by <span class="text-text-primary font-medium">Prdko Jistic</span> — Founder, AgentMe</p>
      <div class="flex flex-wrap items-center justify-center gap-x-5 gap-y-1 text-xs text-text-muted">
        <a href="mailto:prdko@agentme.cz" class="transition-colors hover:text-accent-cyan">prdko@agentme.cz</a>
        <span class="hidden sm:inline">·</span>
        <a href="tel:+420910926996" class="transition-colors hover:text-accent-cyan">+420 910 926 996</a>
        <span class="hidden sm:inline">·</span>
        <span>Prague, Karlín 🇨🇿</span>
      </div>
    </div>
  </div>
</footer>
